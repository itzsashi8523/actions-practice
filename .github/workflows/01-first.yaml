name: hellow_world
on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome Message
          run: echo "Hello Everyone"
        - name: list files
          run: ls -la
        - name: Readme file
          run: cat README.md