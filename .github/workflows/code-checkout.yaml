name: code checkout
on: push
jobs:
  code-checkout:
    runs-on: ubuntu-latest
    steps:
      - name: pull the code from github
        uses: actions/checkout@v3
      - name: list the cloned repo
        run: ls -al
