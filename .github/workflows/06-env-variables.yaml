name: print-env-variables
on:
  push:
    branches:
      - develop
env:
  DB_NAME: my-test-db
  DB_USER: sasi
  DB_PWD: S@sik@nth123
jobs:
  display-variables:
    runs-on: ubuntu-latest
    steps:
      - name: print envs
        run: |
            echo ${{ DB_NAME }}
            echo $DB_USER
            echo $DB_PWD