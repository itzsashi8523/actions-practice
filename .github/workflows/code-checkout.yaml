name: code-checkout
on: push
jobs:
  clone-a-repo:
    runs-on: ubuntu-latest
    steps:
      - name: clone the repo from github
        uses: actions/checkout@v3
        with:
          ref: 'develop'
      - name: check where are you now
        run: pwd
      - name: list the repo folder
        run: ls -la
