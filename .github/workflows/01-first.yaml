name: first-job
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: using env variables
        run: useradd tom
      - name: Using env variable
        run: echo "Hi $USER"